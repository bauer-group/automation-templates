# 📄 README-Generator Beispiel für externe Projekte
# Kopiere diese Datei als .github/workflows/readme.yml in dein Projekt

name: 📄 README Documentation

on:
  push:
    branches: [ main ]
    paths: 
      - 'docs/README.template.MD'
      - '.github/workflows/readme.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'docs/README.template.MD'
  schedule:
    # Täglich um 02:00 UTC für automatische Updates
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      force-update:
        description: "README auch ohne Änderungen aktualisieren"
        type: boolean
        default: false

jobs:
  readme:
    uses: bauer-group/automation-templates/github/workflows/readme.yml@main
    with:
      template-path: "docs/README.template.MD"
      output-path: "README.MD"
      project-name: "Mein Projekt"
      company-name: "Meine Firma"
      project-description: "Beschreibung meines Projekts"
      contact-email: "support@example.com"
      documentation-url: "https://docs.example.com"
      support-url: "https://support.example.com"
      force-update: ${{ inputs.force-update || false }}
      validate-output: true
      auto-commit: true

# 📋 Verwendung:
# 1. Erstelle eine Datei docs/README.template.MD mit Platzhaltern
# 2. Verfügbare Platzhalter:
#    - {{PROJECT_NAME}} - Name des Projekts
#    - {{VERSION}} - Aktuelle Version aus Git Tags
#    - {{DATE}} - Aktuelles Datum
#    - {{COMPANY_NAME}} - Firmenname
#    - {{PROJECT_DESCRIPTION}} - Projektbeschreibung
#    - {{REPO_URL}} - Repository URL
#    - {{CURRENT_BRANCH}} - Aktueller Branch
#    - {{CONTACT_EMAIL}} - Kontakt E-Mail
#    - {{DOCUMENTATION_URL}} - Dokumentations-URL
#    - {{SUPPORT_URL}} - Support-URL
# 3. Der Workflow wird automatisch bei Änderungen am Template ausgeführt
# 4. Manuelle Ausführung über Actions-Tab möglich
